# Session record, 22 Sep 2026: release day (Geist refresh, system page, brand pass)

## Shipped to main
- #98 Geist refresh: styles, Drift, /work, Theming.
- #99 hotfix: Vercel's restored `.next/cache` shipped the pre-#98 `globals.css`
  with the #98 markup; Drift was 79,429px tall. `vercel-build` now clears the
  cache; the people-row SVGs carry intrinsic sizes; `audit:frame` fails any
  page taller than 20,000px at 1440.
- #100 W1: /design-system rebuilt from the mock, CaseBackLink, CaseRow for Home
  and /work, live fixes A1 to A7.

## In review: feat/site-brand (W3)
BELLA sync at d99421e, purple to ochre/ink, `--layout-max` 66rem, "Let's talk"
with the ink primary, the 7x3 "21" hero visual, playground panel height, nav
and footer BrandWordmark (dog and paw trail removed), PatternField hero
ground, CHIP h2s on the display-2 tier.

## Tonight's leftover list
1. BELLA `feat/brand-ochre`: push and open its PR. The site vendors d99421e,
   which exists only on this Mac until then. Re-sync if BELLA changes after it.
2. Favicon: pattern E on cream from BELLA, once the seed is locked (skipped).
3. /design-system h1 full stop: "...AI can build with." is 51 characters and
   `audit:frame` caps h1 at 50. Elleta to pick: shorter copy or a higher cap.
4. Theming case content still shows BELLA iris (the brand ramp and the
   focus-ring row in `components/theming/themes.ts`). JS content, not CSS,
   so the built-CSS grep is 0; decide whether the case keeps its history or
   moves to ochre.
5. PatternField is barely visible in dark (6% plus the fade), notably at 390.
   Check against the mock.
6. The footer wordmark sits about 20px inside the container edges (the SVG's
   side bearings). Check against footer-mock.html.
7. BELLA Button `font-family` resolves a component token to a literal Geist
   stack; the site audits accept it only for Geist. Consider a font token
   upstream.
8. W4: /about restyle to the new language (no new content).
9. Remove leftover worktrees: -system-page, -hotfix, -site-brand once merged,
   and the scratch BELLA checkout.
10. The pre-commit hook (local, untracked) now allowlists 2147483647 and
    2147483646, BELLA's random-number modulus, not phone numbers.
