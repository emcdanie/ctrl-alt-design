/**
 * SectionWrapper
 *
 * Canonical layout primitive for homepage and case study sections.
 * Provides consistent vertical spacing and a responsive max-width container.
 *
 * This is a named re-export of ui/SectionShell — use whichever name reads better
 * in context. Both are identical in behaviour.
 *
 * Usage:
 *   <SectionWrapper id="work" className="bg-[#F8F5F0]">
 *     <SectionHeader label="— Work" title="Selected Case Studies" />
 *     ...
 *   </SectionWrapper>
 */
export { default } from "@/components/ui/SectionShell";
