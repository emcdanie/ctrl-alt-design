# Two-speed flow + polish — tasks

- [x] Baseline screenshots (home 1440 + 390). (done)
- [x] Home: delete current-focus section + Hero onEnterDashboard; hero door two
      quiet link → /quick.
- [x] WorkLibrary: remove TimelineView + tl* CSS; add CardsView (CaseCard grid);
      VIEWS = table/map/cards/skills; Select on map/cards; export MatrixView.
- [x] /quick route composed from existing components.
- [x] Colophon ’ fix + testimonial — fix; audit:copy escape checks.
- [x] scripts/audit-reuse.mjs + gate wiring; delete ui/SurfaceCard.
- [x] Gate green, tsc, routes 200 light+dark, after screenshots (home doors, /quick,
      switcher, desktop + 390).
