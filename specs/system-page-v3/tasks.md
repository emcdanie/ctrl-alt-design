# System page v3 — tasks (commit per task, gate green between)

- [x] T0 spec committed (this folder)
- [x] T1 one ground: retire ds-band--card + per-band surfaces; identity
      wash recorded exception; audit:visual band assertion
- [x] T2 cards carry structure: content units onto ui/Card; Rules stays a
      list; TYPE display specimens on the ground (recorded exception);
      Unique-in-card enforcement moves to runtime audit:type
- [x] T3 map descriptions: nine TODO(elleta) rail slots, layout holds
      empty/filled (tenth named by Elleta if the count was literal)
- [x] T4 How the gate works: per-audit cards + receipt slots (parity, axe,
      CI harness), receipts section absorbed, not-covered-yet line,
      status card rows with the real coming list, PASS-chip grid gone
- [x] T5 specimen alignment: one grid recipe, fixed head slot, centred
      body, pinned toggle; equal-height assertion in audit:visual
- [x] T6 redline annotations: flag overlay + per-band toggle, metadata
      tier recorded, AA both themes, no overlap 1440/390; inspector keeps
      the readout mode
- [x] T7 verify: gate 13/13, tsc, routes, shots incl. band before/after +
      annotated states; PR + preview; STOP with the TODO slot list
