# Requirements: case curation + case shell v2

## Phase A

A1. content/case-studies/_archive/ holds guardian.ts, un-operational-dashboard.ts,
    filters-decision-support-system.ts, design-lab.ts, full content intact, each
    with an ARCHIVED header comment (deliberately unrouted, Elleta 22 Jul 2026).
A2. index.ts exports exactly the three star cases; /case-studies routes exist for
    exactly those three; archived routes 404.
A3. WORK_ITEMS carries exactly three case-study rows + the Design Lab row; parity,
    llms.txt, /api/bella.json, /work counts, fit corpus, matrix, related rows all
    regenerate consistently from it.
A4. No dead links, no dead routes, ASSERTED: every internal href on every gate
    route resolves 200; the three archived route strings appear in no rendered
    page.
A5. Bubble cluster: hive composition intact; chip/code-first/drift spheres link
    to their cases; guardian/clarity spheres are non-interactive topic
    identities without links; design-lab and hub keep their live links.
A6. Untouched: Find My Fit (search + fit API), Cards/Map/Table switcher, Design
    Lab band and its 8 pieces, employment history content.
A7. audit-reuse EXEMPT covers content/case-studies/_archive/ by design.
A8. Gate 13/13 + tsc clean; all remaining routes 200 both themes; home + /work
    screenshots (1440/390, both themes) in _review/after/case-shell-v2/.
A9. PR opened, STOP at preview. Phase B starts only on Elleta's go.

## Phase B (on her go)

B1. Code First renders on shell v2: in-flow title (no sticky side title), one
    idea per viewport, text beside visual, annotated before-state opener, live
    parity specimen with anchored flags, dark-flip payoff section, clips only
    with 3-bullet summaries, 16px reading tier, TODO(elleta) for new prose.
B2. chip + drift keep the old shell; no old-shell code deleted while they use it.
B3. /skills defaults to the grouped readable list (skill, one-liner TODO slot,
    "proven in" link); matrix at most a secondary toggle.
B4. Gate 13/13 + tsc; screenshots of /work, home, Code First, /skills (1440/390,
    both themes) in _review/after/case-shell-v2/. Same PR, STOP at preview.
