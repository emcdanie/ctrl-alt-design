{
  "$comment": "Accepted npm audit advisories (high/critical only reach the gate). Each entry needs id (GHSA), reason, expires (YYYY-MM-DD). Empty by design — see scripts/npm-audit-gate.mjs.",
  "advisories": []
}
